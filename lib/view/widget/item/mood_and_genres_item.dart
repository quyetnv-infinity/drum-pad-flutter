import 'package:flutter/material.dart';

class MoodAndGenresItem extends StatelessWidget {
  const MoodAndGenresItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,

    );
  }
}
